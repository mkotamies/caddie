# Build Environment
build_environment: Ubuntu 12.04

# language setting
language: node_js

# version numbers
node_js:
  - "0.11"

install: 
  - cd src/main/resources/static && npm install
  
#run tests  
script:
  - cd src/main/resources/static && npm test